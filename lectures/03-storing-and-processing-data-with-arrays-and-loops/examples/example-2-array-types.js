// Empty array - ready to be filled with test data
let testResults = [];
console.log("Empty test results:", testResults);

// Array with test case names
let testCases = [
  "login_test",
  "logout_test",
  "password_reset",
  "profile_update",
];
console.log("Test cases:", testCases);

// Array with different data types (all valid in JavaScript)
let testData = ["user123", 25, true, "active"];
console.log("Mixed test data:", testData);

// Array with expected status codes
let expectedCodes = [200, 201, 204];
console.log("Expected codes:", expectedCodes);

// Array with error messages to validate
let errorMessages = [
  "Username is required",
  "Password must be at least 8 characters",
  "Email format is invalid",
];
console.log("Error messages:", errorMessages);
